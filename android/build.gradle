dependencies {
    // React Native dependencies
    implementation 'com.facebook.react:react-native:+'

    // Kotlin dependencies
    implementation "org.jetbrains.kotlin:kotlin-stdlib:$kotlinVersion"

    // AndroidX dependencies (replace support libraries with AndroidX)
    implementation 'androidx.core:core:1.13.1'

    // Remove the old support-compat library if you have it
    // implementation 'com.android.support:support-compat:27.1.1' <-- REMOVE THIS
}
